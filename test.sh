#!/bin/bash
# Test script to run game and capture output for debugging

echo "Starting game with debug output..."
echo "Move with WASD and watch the console for debug info"
echo "====================================="

cd "$(dirname "$0")"
./src/P1600_1977
