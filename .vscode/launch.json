{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenGL Game",
            "type": "node",
            "request": "launch",
            "program": "${file}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}